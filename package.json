{
  "name": "helpdesk-web",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend consumidor da HelpDesk API",
  "scripts": {
    "build": "node build.js",
    "test": "node --check app.js && node --check build.js"
  },
  "engines": {
    "node": ">=22"
  }
}
